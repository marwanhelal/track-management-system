# Track Management System Frontend Configuration

# Development Server Port
PORT=3000

# Backend API URL
# Development: http://localhost:5005/api/v1
# Production: https://api.your-domain.com/api/v1
REACT_APP_API_URL=http://localhost:5005/api/v1

# Backend Socket.IO URL
# Development: http://localhost:5005
# Production: https://api.your-domain.com
REACT_APP_SOCKET_URL=http://localhost:5005

# Build Configuration
GENERATE_SOURCEMAP=false
NODE_OPTIONS=--max-old-space-size=4096
DISABLE_ESLINT_PLUGIN=true
TSC_COMPILE_ON_ERROR=true

# NOTE: In production (NODE_ENV=production), the app will automatically
# upgrade HTTP URLs to HTTPS for security. However, it's recommended to
# set the correct HTTPS URLs in your production environment variables.
